{
	"name": "ziriz-sdk",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"tsc": "tsc --init",
		"build": "tsc",
		"test": "jest",
		"contract:init": "ts-node src/init.ts",
		"contract:test": "cd contract && cargo test",
		"contract:build": "cd contract && cargo build --target wasm32-unknown-unknown --release",
		"contract:deploy-testnet": "cd contract && soroban contract deploy --wasm target/wasm32-unknown-unknown/release/ziriz.wasm --source alice --network testnet",
		"contract:link": "yarn remove contract-client && rm -rf contract-client  && cd contract && soroban contract bindings typescript --network testnet --contract-id CCSIZSPJU5MSE2FJGYYLFVGP6S42LLSW2YJPAKASRIW72D6QS7YUR4OF --output-dir ../contract-client",
		"contract:add": "yarn add file:./contract-client"
	},
	"author": "dev@paras.id",
	"license": "ISC",
	"devDependencies": {
		"@types/jest": "^29.5.11",
		"jest": "^29.7.0",
		"remove": "^0.1.5",
		"ts-jest": "^29.1.1",
		"ts-node": "^10.9.2",
		"tsc": "^2.0.4",
		"typescript": "^5.3.3"
	},
	"dependencies": {
		"axios": "^1.6.5",
		"bip39": "^3.1.0",
		"contract-client": "file:./contract-client",
		"dotenv": "^16.3.1",
		"stellar-sdk": "^11.1.0"
	}
}
